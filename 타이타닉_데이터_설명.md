# 타이타닉 데이터 설명

## 1. 데이터 개요

`titanic.csv`는 타이타닉호 승객 891명의 정보를 담은 표 형태의 데이터입니다.

- 행(row) 수: 891개
- 열(column) 수: 12개
- 한 행의 의미: 승객 1명의 기록
- `Survived`는 생존 여부를 나타내는 컬럼이며, 나머지 컬럼에는 승객의 기본 정보와 승선 정보가 들어 있습니다.

이 문서는 데이터의 구성과 컬럼의 뜻만 설명합니다. 어떤 조건이 생존에 영향을 주었는지에 대한 분석이나 예측은 다루지 않습니다.

## 2. 컬럼 설명

| 컬럼 | 의미 | 값의 형태 또는 예시 | 비고 |
|---|---|---|---|
| `PassengerId` | 승객 식별 번호 | 1~891 | 각 승객을 구분하는 번호이며, 891개 값이 모두 고유합니다. |
| `Survived` | 생존 여부 | 0, 1 | 0은 사망, 1은 생존을 뜻합니다. |
| `Pclass` | 객실 등급 | 1, 2, 3 | 1은 1등석, 2는 2등석, 3은 3등석입니다. |
| `Name` | 승객 이름 | `Braund, Mr. Owen Harris` | 성명뿐 아니라 `Mr`, `Mrs`, `Miss` 같은 호칭도 포함될 수 있습니다. |
| `Sex` | 성별 | `male`, `female` | `male`은 남성, `female`은 여성입니다. |
| `Age` | 나이 | 0.42~80 | 단위는 년입니다. 1세 미만 승객은 소수로 표시될 수 있습니다. |
| `SibSp` | 함께 탑승한 형제·자매 및 배우자 수 | 0~8 | 형제, 자매, 의붓형제·자매, 남편 또는 아내의 수입니다. |
| `Parch` | 함께 탑승한 부모 및 자녀 수 | 0~6 | 어머니, 아버지, 딸, 아들의 수입니다. |
| `Ticket` | 승차권 번호 | `A/5 21171`, `PC 17599` | 숫자만 있거나 문자와 숫자가 함께 있을 수 있습니다. 같은 승차권 번호를 여러 승객이 공유하기도 합니다. |
| `Fare` | 승차권 요금 | 0~512.3292 | 화폐 단위로 기록된 요금입니다. |
| `Cabin` | 객실 번호 | `C85`, `C123` | 객실 정보가 없는 행이 많으며, 한 승객에게 여러 객실 번호가 적힌 경우도 있습니다. |
| `Embarked` | 승선 항구 | `C`, `Q`, `S` | C는 Cherbourg, Q는 Queenstown, S는 Southampton입니다. |

## 3. 주요 범주별 기록 수

아래 숫자는 단순히 각 값이 데이터에 몇 번 나타나는지 보여 줍니다.

### 생존 여부 (`Survived`)

| 값 | 뜻 | 기록 수 |
|---|---|---:|
| 0 | 사망 | 549 |
| 1 | 생존 | 342 |

### 객실 등급 (`Pclass`)

| 값 | 뜻 | 기록 수 |
|---|---|---:|
| 1 | 1등석 | 216 |
| 2 | 2등석 | 184 |
| 3 | 3등석 | 491 |

### 성별 (`Sex`)

| 값 | 뜻 | 기록 수 |
|---|---|---:|
| `female` | 여성 | 314 |
| `male` | 남성 | 577 |

### 승선 항구 (`Embarked`)

| 값 | 뜻 | 기록 수 |
|---|---|---:|
| `C` | Cherbourg | 168 |
| `Q` | Queenstown | 77 |
| `S` | Southampton | 644 |
| 빈칸 | 정보 없음 | 2 |

## 4. 결측치

결측치는 값이 기록되지 않아 빈칸으로 남아 있는 경우를 뜻합니다.

| 컬럼 | 결측치 수 | 전체 891개 중 비율 |
|---|---:|---:|
| `Age` | 177 | 약 19.9% |
| `Cabin` | 687 | 약 77.1% |
| `Embarked` | 2 | 약 0.2% |

나머지 9개 컬럼에는 결측치가 없습니다. 특히 `Cabin`은 빈칸이 매우 많으므로 데이터를 읽을 때 빈 값을 실제 객실 번호처럼 해석하지 않도록 주의해야 합니다.

## 5. 숫자형 컬럼의 간단한 범위

| 컬럼 | 최솟값 | 최댓값 | 평균 |
|---|---:|---:|---:|
| `PassengerId` | 1 | 891 | 446.00 |
| `Age` | 0.42 | 80 | 약 29.70 |
| `Fare` | 0 | 512.3292 | 약 32.20 |

`Age`의 평균은 나이가 기록된 714명만을 기준으로 한 값입니다. 이 표는 값의 대략적인 범위를 이해하기 위한 것이며, 원인이나 관계를 설명하는 분석 결과는 아닙니다.

## 6. 데이터를 볼 때 알아둘 점

- `PassengerId`는 승객을 구분하기 위한 번호이지, 나이·등급 같은 승객 특성을 뜻하지 않습니다.
- `Survived`, `Pclass`, `Embarked`는 숫자나 한 글자로 표현되지만 각각 정해진 범주를 의미합니다.
- `Age`, `Cabin`, `Embarked`에는 빈 값이 있습니다.
- `Ticket`과 `Cabin`은 한 값의 모양이 일정하지 않을 수 있습니다.
- `SibSp`와 `Parch`는 가족의 전체 인원수가 아니라 승객 본인을 제외한 특정 관계의 동반 인원수입니다.
